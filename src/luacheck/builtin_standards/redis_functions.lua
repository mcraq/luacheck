local empty = {}

local redis_functions = {
    fields = {
        redis = {
            fields = {
                register_function = empty,
            },
        }
    }
}

return redis_functions
